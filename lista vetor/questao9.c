#include <stdio.h>

int main(void){

    int cadastro[50], i=0, j;

    do{
        j==0;
        printf("Informe o cadastro: ");
        scanf("%d", &j);
        cadastro[i] == j;
        i++;
    }while(j!=0);

    printf("%d", cadastro[0]);

    return 0;
}
